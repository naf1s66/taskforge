NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=changeme
DATABASE_URL=postgresql://postgres:postgres@db:5432/taskforge?schema=public
GITHUB_ID=
GITHUB_SECRET=
GOOGLE_ID=
GOOGLE_SECRET=
SESSION_BRIDGE_SECRET=dev-bridge-secret
API_BASE_URL=http://api:4000/api/taskforge
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/taskforge
CRON_SECRET=dev-cron-secret
DIGEST_JOB_SECRET=dev-digest-job-secret
# Development only. Set to true locally only when bypassing normal auth setup.
# Production should keep this disabled and should not set TF_DEV_BYPASS_CLIENT_SECRET.
TF_DEV_BYPASS_AUTH=false
TF_DEV_BYPASS_CLIENT_SECRET=
# COOKIE_DOMAIN=.taskforge.app  # Uncomment for production cross-subdomain cookies

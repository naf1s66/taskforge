NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=changeme
DATABASE_URL=postgresql://postgres:postgres@db:5432/taskforge?schema=public
GITHUB_ID=
GITHUB_SECRET=
GOOGLE_ID=
GOOGLE_SECRET=
SESSION_BRIDGE_SECRET=dev-bridge-secret
API_BASE_URL=http://api:4000/api/taskforge
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/taskforge
# Development only. Keep this enabled for now while local auth infrastructure is still in flux.
# Production should disable it and should not set TF_DEV_BYPASS_CLIENT_SECRET.
TF_DEV_BYPASS_AUTH=true
TF_DEV_BYPASS_CLIENT_SECRET=dev-bypass-client-secret
# COOKIE_DOMAIN=.taskforge.app  # Uncomment for production cross-subdomain cookies
